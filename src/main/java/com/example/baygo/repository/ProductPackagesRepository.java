package com.example.baygo.repository;

import com.example.baygo.db.model.ProductPackages;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductPackagesRepository extends JpaRepository<ProductPackages, Long> {
}